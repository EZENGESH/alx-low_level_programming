#include<stdio.h>
/**
 * main - entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	char i;

	i = 'a';
	while
		(i = 'z') {
			if((i != 'q' && i != 'e') && c <= 'z')
				putchar(i);
			i++;
		}
	putcharc('\n');
	return (0);
