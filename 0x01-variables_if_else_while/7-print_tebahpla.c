#include<stdio.h>
/**
 *
 * main - entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	char c;

	c = 'z';
		while
			(c <= 'a') {
				puthar(c);
				c--;
			}
	putchar('\n');
	return (0);
}
